# TODO:
# use tokyonight colours imported from tokyonight.nvim
# remove tpm from all scripts and try get as far as you can without it
# use XDG_CONFIG file layout

# OPTIONS

set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"
set -g history-limit 100000
set-option -sg escape-time 10
set-option -g focus-events on
set-option -g renumber-windows on

#KEYBINDS

# prefix 
unbind C-b
set -g prefix C-Space

# easily source config 
unbind r
bind r source-file ~/.tmux.conf \; display "Reloaded ~/.tmux.conf"

# window navigation
unbind j
bind j next-window
unbind k
bind k previous-window

# vim keybinds in copy mode
set-window-option -g mode-keys vi

#toggle status bar
unbind s
bind s set -g status

# clear history
unbind e
bind e clear-history

# PLUGINS

# list of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'janoamaral/tokyo-night-tmux'


#auto-install tpm
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"

# initialize tpm
run '~/.tmux/plugins/tpm/tpm'

# APPEARANCE 

# customizing status line
set -g status-right ''
set -g status-left ''
set -g status-bg black

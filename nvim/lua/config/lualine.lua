require("lualine").setup({
    options = {
        icons_enabled = false,
        theme = require("variables").lualinetheme,
        component_separators = "|",
        section_separators = "",
        globalstatus = true,
    },
})
